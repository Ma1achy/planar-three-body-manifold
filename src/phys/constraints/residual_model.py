# Defines the residual vector you want to drive to zero (e.g. COM=0, P=0, E-E0=0, L-L0=0) and evaluates it for a given parameterization of “free variables”. Centralizes “what constraints mean” so they’re not duplicated across charts.
# class ResidualModel: residual vector definition/eval
