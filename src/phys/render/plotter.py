# Debug plotting utilities: show textures, masks, histograms of residuals, and optionally a few trajectory plots for selected pixels. This should be optional and never required for running batch renders.
# class Plotter: debug plots (textures + example trajectories)
