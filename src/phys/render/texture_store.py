# Handles saving/loading textures plus metadata (view spec, channel definitions, hashes, failure masks). Keep file formats simple (NPZ + JSON) until you need something heavier.
# class TextureStore: save/load textures + metadata
