# Detects collision/escape/close-approach events during integration and returns an event label + time index. Keeps event logic separate from the integrator so you can swap integrators without rewriting events.
# class EventDetector: collision/escape/close approach
