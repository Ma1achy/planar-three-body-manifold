# Maps chart names (strings) to concrete chart classes/instances and enforces a consistent chart interface. Keeps chart selection logic out of the decoder and view compiler.
# class ChartRegistry: name -> chart instance
