# Implements an “affine slice” chart: reads specific latent dims and produces an initial guess for positions/velocities in a chosen coordinate convention. Should not run heavy solvers; just construct a reasonable guess.
# class ChartAffine: initial_guess from x10/spec
