# Stable hashing of ViewSpec (and optionally code version) to uniquely identify outputs and cache entries. This is crucial so “change one variable → new output folder” is automatic.
# class Hasher: stable hash of view/spec/config
