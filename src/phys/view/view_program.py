# Holds the compiled pipeline objects and provides a small, ergonomic interface (e.g. run() or render_textures()), plus access to shared metadata (hash, channel list). Think of it as the “executable view”.
# class ViewProgram: holds assembled pipeline + convenience methods
