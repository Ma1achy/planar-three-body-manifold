# The declarative description of a slice/view: which 2 latent dims are UV axes, fixed values for the other 8, UV ranges/resolution, chart choice, constraint targets, sim settings, and desired output channels. This should be mostly “data + validation”, not execution.
# class ViewSpec: immutable slice definition (uv dims + fixed 8, ranges, res, etc.)
