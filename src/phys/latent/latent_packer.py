# Builds an x10 vector from a ViewSpec + (u,v) by inserting UV into chosen indices and filling the other 8 fixed dims. Also handles dict↔vector packing/unpacking for readability/debugging.
# class LatentPacker: assemble x10 from fixed + (u,v)
