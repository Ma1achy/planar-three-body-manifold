# Stores bounds/ranges per latent dimension and provides validate(x10) and clamp(x10) utilities. Useful for guarding against nonsensical views and keeping UV→latent mappings stable.
# class LatentBounds: bounds + validate/clamp
